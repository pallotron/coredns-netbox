apiVersion: v1
kind: Service
metadata:
  name: {{ include "coredns-netbox.fullname" . }}-headless
  labels:
    {{- include "coredns-netbox.labels" . | nindent 4 }}
spec:
  clusterIP: None
  selector:
    {{- include "coredns-netbox.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: primary
